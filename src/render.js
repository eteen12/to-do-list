import buttonFunction from './button';

const renderFunc = () => {
    
    function render(){

        const renderTask = [];

        for (let i =0; i < renderTask.length;i+){

            let taskWeapper
        }
    }
}
export default renderFunc;